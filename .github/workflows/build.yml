name: build

on:
  push:
    branches:
      - main
      - windows_support
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_wheels:
    name: cibuildwheel on ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    env:
      BLA_VENDOR: OpenBLAS
      LMGC90_GIT_URL: ${{ secrets.LMGC90_GIT_URL }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: manylinux
          - os: windows-latest
            platform: windows
          # Pin to macos-14 (Apple Silicon, macOS 14 / Sonoma) so Homebrew
          # bottles target macOS 14 and our wheel doesn't end up requiring
          # macOS 15 just because the runner is on the bleeding edge. Keep
          # in sync with MACOSX_DEPLOYMENT_TARGET in pyproject.toml.
          - os: macos-14
            platform: macos

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gfortran cmake libopenblas-dev liblapack-dev

      - name: Install cibuildwheel
        run: pipx install cibuildwheel==2.23.1

      - name: Build wheels
        run: cibuildwheel --output-dir wheelhouse .

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.platform }}
          path: wheelhouse/*.whl

  build_sdist:
    name: Test source distribution
    runs-on: ubuntu-latest
    env:
      LMGC90_GIT_URL: ${{ secrets.LMGC90_GIT_URL }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build SDist
        run: pipx run build --sdist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz